<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qrcodemall.dao.PromotionMapper">
    <resultMap id="BaseResultMap" type="com.qrcodemall.entity.PromotionGoods">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="promotion_id" jdbcType="INTEGER" property="promotionId" />
        <result column="goods_id" jdbcType="INTEGER" property="goodsId" />
        <result column="promotion_start_time" jdbcType="TIMESTAMP" property="promotionStartTime" />
        <result column="promotion_duration" jdbcType="TIMESTAMP" property="promotionDuration" />
        <result column="promotion_count" jdbcType="INTEGER" property="promotionCount" />
        <result column="promotion_value" jdbcType="DECIMAL" property="promotionValue" />
        <result column="is_deleted" jdbcType="INTEGER" property="isDeleted" />
    </resultMap>
    <sql id="Base_Column_List">
        promotion_id,goods_id, promotion_start_time, promotion_duration, promotion_count, promotion_value,
        is_deleted
    </sql>

    <select id="select" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from promotion
        <where>
            <if test="promotionId != null">
                and promotion_id = #{promotionId}
            </if>
            <if test="goodsId != null">
                and goods_id = #{goodsId}
            </if>
            <if test="promotionStartTime != null">
                and promotion_start_time = #{promotionStartTime}
            </if>
            <if test="promotionDuration != null">
                and promotion_duration = #{promotionDuration}
            </if>
            <if test="promotionCount != null">
                and promotion_count = #{promotionCount}
            </if>
            <if test="promotionValue != null">
                and promotion_value = #{promotionValue}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>

        </where>
    </select>

    <select id="selectLatestPromotionByGoodsId" resultMap="BaseResultMap">
        select max(promotion_id) from promotion where
                goods_id = #{goodsId,jdbcType=INTEGER} and is_deleted = 0
    </select>

    <insert id="insert" parameterType="com.qrcodemall.entity.PromotionGoods">
        insert into promotion
                (goods_id,promotion_start_time,promotion_duration,promotion_count,promotion_value,is_deleted)
                    values
             (#{goodsId,jdbcType=INTEGER},
             #{promotionStartTime,jdbcType=TIMESTAMP},
                    #{promotionDuration,jdbcType=INTEGER},#{promotionCount,jdbcType=INTEGER},#{promotionValue,jdbcType=DECIMAL},
                    #{isDeleted,jdbcType=INTEGER})
    </insert>



    <update id="delete">
        update promotion
               set is_deleted = 1 where goods_id = #{goodsId} and is_deleted = 0

    </update>

</mapper>